<phone:PhoneApplicationPage
    x:Class="Kiri.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="Public Transport" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0"/>
            <TextBlock Text="Kiri WP 8" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock Text="From" Style="{StaticResource PhoneTextNormalStyle}" Margin="26,24,278,515" FontSize="50"/>
            <TextBlock Text="To" Style="{StaticResource PhoneTextNormalStyle}" Margin="26,235,361,297" FontSize="50"/>
            <TextBox x:Name="fromBox" HorizontalAlignment="Left" Margin="12,97,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="24" Width="266"/>
            <TextBox x:Name="toBox" HorizontalAlignment="Left" Margin="12,310,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="24" Width="266"/>
            <Button Content="FIND" HorizontalAlignment="Left" Margin="248,195,0,0" VerticalAlignment="Top" Click="startRoute" Height="81" Width="180"/>
            <HyperlinkButton Content="Map" HorizontalAlignment="Left" Margin="283,117,0,0" VerticalAlignment="Top" Click="changeMapFrom" BorderThickness="2" BorderBrush="White" Padding="0" RenderTransformOrigin="0.45,0.516"/>
            <HyperlinkButton Content="Map" HorizontalAlignment="Left" Margin="283,330,0,0" VerticalAlignment="Top" Click="changeMapTo" BorderThickness="2" BorderBrush="White" Padding="0"/>
            <TextBlock HorizontalAlignment="Left" Margin="26,411,0,0" TextWrapping="Wrap" Text="Kota" VerticalAlignment="Top"/>
            <toolkit:ListPicker  Height="50" ScrollViewer.VerticalScrollBarVisibility="Auto" ItemsSource="{Binding ElementName=ConverterPage, Path=Locations}" Margin="97,394,152,163" Name="cmbCurrFrom">
                <toolkit:ListPicker.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontSize="30" Foreground="Black" Text="{Binding Path=Location}">Auto</TextBlock>
                    </DataTemplate>
                </toolkit:ListPicker.ItemTemplate>
                <toolkit:ListPicker.FullModeItemTemplate>
                    <DataTemplate>
                        <TextBlock FontSize="30" Foreground="Black" Text="{Binding Path=Location}">Bandung</TextBlock>
                    </DataTemplate>
                </toolkit:ListPicker.FullModeItemTemplate>
            </toolkit:ListPicker>
        </Grid>
        <Button Content="Here" HorizontalAlignment="Left" Margin="368,308,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        <Button Content="Here" HorizontalAlignment="Left" Margin="368,98,0,0" Grid.Row="1" VerticalAlignment="Top" Click="Button_Click"/>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>